data_dir: processed
img_size: 224
batch_size: 32
lr: 0.001
epochs: 10
val_split: 0.2
model_name: resnet18
pretrained: true      # use ImageNet weights
finetune: true        # true = update all layers; false = freeze backbone
num_classes: 5
